name: project_bb
description: Project Brain&Breads

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Network
  http: 0.12.2
  cached_network_image: 2.5.1
  # Class
  equatable: 1.2.6
  # State management
  bloc: 6.1.3
  flutter_bloc: 6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true